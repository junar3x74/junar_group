<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- White background color -->
    <corners android:radius="20dp" /> <!-- Rounded corners -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />
    <shadow
        android:color="#50000000"
        android:radius="8dp"
        android:dx="0dp"
        android:dy="4dp" /> <!-- Optional: Drop shadow -->
</shape>
